# name: Deploy to Production
# 
# on:
#   push:
#     branches:
#       - main
# 
# jobs:
#   lint-and-build:
#     name: Lint and Build Check
#     runs-on: ubuntu-latest
# 
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
# 
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '24'
# 
#       - name: Install dependencies
#         run: npm install
# 
#       - name: Run ESLint
#         run: npm run lint
# 
#       - name: Check TypeScript types
#         run: npm run build:prod
# 
#   deploy:
#     name: Deploy to Production Server
#     runs-on: ubuntu-latest
#     needs: lint-and-build
# 
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
# 
#       - name: Deploy to Production Server
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.PRODUCTION_SERVER_HOST }}
#           username: ${{ secrets.PRODUCTION_SERVER_USER }}
#           key: ${{ secrets.SSH_PRIVATE_KEY }}
#           port: ${{ secrets.SSH_PORT || 22 }}
#           script: |
#             cd /root/api-boilerplate-api
# 
#             echo "Pulling latest changes from main branch..."
#             git fetch origin main
#             git reset --hard origin/main
# 
#             echo ".env.production should already exist on the server (not created from secrets)"
# 
#             echo "Starting infrastructure services..."
#             docker compose -f docker-compose-production.yml up -d postgres-production minio-production redis-production loki-production prometheus-production grafana-production
# 
#             echo "Waiting for postgres to be ready..."
#             sleep 15
# 
#             echo "Creating backup of current deployment..."
#             docker compose -f docker-compose-production.yml exec -T postgres-production pg_dump -U ${{ secrets.PRODUCTION_DB_USERNAME }} ${{ secrets.PRODUCTION_DB_DATABASE }} > backup_$(date +%Y%m%d_%H%M%S).sql || true
# 
#             echo "Running database migrations (if container exists)..."
#             docker exec api-boilerplate-production npm run migration:run || echo "Container not yet available, will run migrations after deployment"
# 
#             echo "Building new API image..."
#             docker compose -f docker-compose-production.yml build api-boilerplate-production
# 
#             echo "Performing zero-downtime deployment..."
#             docker compose -f docker-compose-production.yml up -d --build api-boilerplate-production
# 
#             echo "Waiting for API container to be healthy..."
#             sleep 15
# 
#             echo "Verifying new container is running..."
#             docker ps | grep api-boilerplate-production
# 
#             echo "Removing old stopped containers..."
#             docker container prune -f
# 
#             echo "Cleaning up old images..."
#             docker image prune -f
# 
#             echo "Deployment completed successfully!"
# 
#             echo "Service status:"
#             docker compose -f docker-compose-production.yml ps
# 
#       - name: Notify Success
#         if: success()
#         run: echo "✅ Production deployment successful!"
# 
#       - name: Show Container Logs on Failure
#         if: failure()
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.PRODUCTION_SERVER_HOST }}
#           username: ${{ secrets.PRODUCTION_SERVER_USER }}
#           key: ${{ secrets.SSH_PRIVATE_KEY }}
#           port: ${{ secrets.SSH_PORT || 22 }}
#           script: |
#             echo "Showing container logs for debugging..."
#             docker logs api-boilerplate-production --tail 100 || echo "Could not retrieve logs"
# 
#       - name: Notify Failure
#         if: failure()
#         run: echo "Production deployment failed!"
