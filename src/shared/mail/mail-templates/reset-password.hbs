<!DOCTYPE html>
<html lang='{{lang}}'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <meta name='color-scheme' content='light' />
    <title>{{t 'common.resetPassword'}}</title>
    {{> styles}}
  </head>
  <body>
    <div class='page'>
      <div class='card'>
        {{> header}}

        <div class='content'>
          <span class='eyebrow'>{{upper (t 'common.resetPassword')}}</span>
          <h1 class='heading'>{{t 'reset-password.text1'}}</h1>

          <p class='lede'>{{t 'reset-password.text2'}}</p>

          <div class='code-panel'>
            <span class='code-label'>{{upper (t 'common.resetPassword')}}</span>
            <span class='code'>{{hash}}</span>
          </div>

          <p class='muted'>{{t 'reset-password.text3'}}</p>
        </div>

        {{> footer
          footerText=(t 'reset-password.text4')
          allRights=(t 'reset-password.allRights')
        }}
      </div>
    </div>
  </body>
</html>
